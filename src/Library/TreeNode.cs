using System.Collections.Generic;
using System.Collections.ObjectModel;
using System;

namespace Library
{

    /*
    public class TreeNode : Visitor<Persona>
    {
        public int suma;
        public override void Visitar(Node<Persona> node)
        {
            this.suma += node.Value.edad;
            
            foreach (Node<Persona> n in node.Children)
            {
               n.Accept(this);
            }

        }

    }

    */
}